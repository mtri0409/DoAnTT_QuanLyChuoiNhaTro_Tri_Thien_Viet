services:
  # 0. Postgres for LangGraph checkpointer
  ai_checkpoint:
    image: postgres:15-alpine
    container_name: qlchuoiphongtro_ai_checkpoint
    restart: always
    environment:
      POSTGRES_DB: ai_checkpoint
      POSTGRES_USER: ai
      POSTGRES_PASSWORD: ${AI_DB_PASSWORD:-ai_password}
    ports:
      - "5433:5432"
    volumes:
      - ai_checkpoint_data:/var/lib/postgresql/data
    networks:
      - qlchuoiphongtro_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai -d ai_checkpoint"]
      interval: 5s
      timeout: 40s
      retries: 5
      start_period: 30s

  # 1. MySQL Database Container
  db:
    image: mysql:8.0
    container_name: qlchuoiphongtro_db
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 40s
      retries: 5
      start_period: 30s

  # 2. Spring Boot Backend Service
  backend:
    build:
      context: ./backend/qlchuoiphongtro
      dockerfile: Dockerfile
    container_name: qlchuoiphongtro_backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:mysql://db:3306/${DB_NAME}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      UPLOAD_PATH: ${UPLOAD_PATH}
      AI_OCR_URL: http://ai-services:8000/api
      AI_SERVICE_URL: http://ai-services:8000
      JAVA_BACKEND_URL: http://backend:8080
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads

  # 3. AI Services (FastAPI/YOLO)
  ai-services:
    build:
      context: ./AI_Services
      dockerfile: Dockerfile
    container_name: qlchuoiphongtro_ai
    restart: always
    ports:
      - "8000:8000"
    environment:
      API_BACKEND_URL: http://backend:8080/api/v1/internal/ai-tools
      AI_CHECKPOINT_URL: postgresql://ai:${AI_DB_PASSWORD:-ai_password}@ai_checkpoint:5432/ai_checkpoint
    depends_on:
      - backend
      - ai_checkpoint
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - qlchuoiphongtro_network

  # 4. VNPay Proxy Mock Server
  vnpay:
    build:
      context: ./vnpay
      dockerfile: Dockerfile
    container_name: qlchuoiphongtro_vnpay
    restart: always
    ports:
      - "3001:3001"
    environment:
      VNP_TMN_CODE: ${VNP_TMN_CODE}
      VNP_HASH_SECRET: ${VNP_HASH_SECRET}
      VNP_RETURN_URL: http://${VPS_HOST}:5174
      BACKEND_API_URL: http://backend:8080
      PORT: 3001

  # 5. Frontend Admin Web App
  admin-web:
    build:
      context: ./frontend/QuanLyChuoiNhaTro_Tri_Thien_Viet_Admin
      dockerfile: Dockerfile
    container_name: qlchuoiphongtro_admin_web
    restart: always
    ports:
      - "5173:80"

  # 6. Frontend Tenant Web App
  tenant-web:
    build:
      context: ./frontend/QuanLyNhaTro_Tri_Thien_Viet_Tenant
      dockerfile: Dockerfile
    container_name: qlchuoiphongtro_tenant_web
    restart: always
    ports:
      - "5174:80"

  # 7. Frontend User Web App
  user-web:
    build:
      context: ./frontend/QuanLyChuoiNhaTro_Tri_Thien_Viet_User
      dockerfile: Dockerfile
    container_name: qlchuoiphongtro_user_web
    restart: always
    ports:
      - "5175:80"

  # 8. phpMyAdmin (Web UI to manage MySQL database)
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: qlchuoiphongtro_phpmyadmin
    restart: always
    ports:
      - "8082:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 0
      PMA_ALLOW_EMPTY_PASSWORD: "yes"
    depends_on:
      db:
        condition: service_healthy

volumes:
  mysql_data:
    name: qlchuoiphongtro_mysql_data
  ai_checkpoint_data:
    name: qlchuoiphongtro_ai_checkpoint_data

networks:
  qlchuoiphongtro_network:
    name: qlchuoiphongtro_net
    driver: bridge

